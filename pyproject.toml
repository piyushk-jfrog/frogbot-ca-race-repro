[tool.poetry]
name = "dashboard-ui"
version = "0.1.0"
description = "Dashboard UI Backend"
authors = ["test <test@test.com>"]

[tool.poetry.dependencies]
python = "^3.10"
pyyaml = "5.1"
jinja2 = "2.11.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
